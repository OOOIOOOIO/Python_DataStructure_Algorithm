"""
    파이썬에서 제공하는 표준 자료구 : 리스트, 튜플, 딕셔너리,

     collections 모듈
    deque : 양쪽이 열려있는 큐구조 (큐 : FIFO)
    defaultdict : 키가 없는 딕셔너리, 컨테이너를 초기화 할 때 default 값을 지정한다.
    Counter
    namedtuple
    OrderedDict

     array 모듈(동일한 데이터 타입, list는 여러가지 데이터 타입)
    array 사용하는 방법


     heepq 모듈(힙 생성, 힙 내부자료 접근..)

     bisect 모듈(정렬된 상태로 요소를 추가, 중복값 처리)

     Queue

     struct(바이너리 자료구조)

     copy(객체 복사)

"""
